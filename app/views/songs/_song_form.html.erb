<%= form_for @song do |f| %>

<%if @song.errors%>
    <% @song.errors.full_messages.each do |full_message|%>
        <%= full_message %>
    <%end%>
<%end%>

<br>

    <%= f.label :title %>
    <%= f.text_field :title %>
    <br>
    <%= f.label :artist %>
    <%= f.text_field :artist %>
    <br>
    <%= f.label :genre %>
    <%= f.text_field :genre %>
    <br>
    <%= f.label :album %>
    <%= f.text_field :album %>
    <br>
    <%= f.label :web_address %>
    <%= f.text_field :web_address %>
    <br>
    <%= f.label :playlist %>
    <%= f.collection_select :playlist_id, Playlist.all, :id, :title, include_blank: "Select Playlist"%>
    <br>
    <%= f.fields_for :playlist, @song.build_playlist do |playlist_fields|%>
        <%= playlist_fields.text_field :title%>
    <% end %>

    <%= f.submit%>
<% end %>